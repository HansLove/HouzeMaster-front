export interface PropertyFilters {
  search: string;
  propertyType: string;
  operationType: string;
  minPrice: string;
  maxPrice: string;
  minBedrooms: string;
  maxBedrooms: string;
  city: string;
  featured: boolean;
}
